{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "cordum:policy-studio:rule:velocity",
  "title": "Velocity rule payload",
  "description": "Rate-limiting rule. Match scopes by tenant/topic/risk; decide is always `throttle` with at least one of the per-window thresholds set.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "name",
    "type",
    "scope",
    "status",
    "version",
    "audit",
    "match",
    "decide"
  ],
  "properties": {
    "id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "type": {
      "const": "velocity",
      "description": "Discriminator for a Rule's match/decide payload schema."
    },
    "scope": {
      "type": "object",
      "description": "Narrows where a rule (or bundle) applies. When `kind` is `global`,\n`value` is unused and may be empty; for the other kinds, `value`\ncarries the corresponding identifier (tenant id, workflow id, edge\nfleet name, edge user id).\n",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Narrows where a rule (or bundle) applies. `global` covers an entire\ndeployment; `tenant`/`workflow` scope to job-side bindings;\n`edge_fleet`/`edge_user` scope to edge-side bindings.\n",
          "enum": [
            "global",
            "tenant",
            "workflow",
            "edge_fleet",
            "edge_user"
          ]
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "kind"
      ]
    },
    "status": {
      "type": "string",
      "description": "Lifecycle state of a Rule. `draft` rules are author-only; `published`\nrules participate in evaluation; `deprecated` rules remain queryable\nfor audit and rollback but are skipped by evaluators.\n",
      "enum": [
        "draft",
        "published",
        "deprecated"
      ]
    },
    "version": {
      "type": "string"
    },
    "audit": {
      "type": "object",
      "description": "Records who created or last updated a Rule or Bundle. The `updated_*`\nfields are zero-valued on first creation and populated by the first\nsubsequent edit.\n",
      "properties": {
        "created_at": {
          "type": "string",
          "format": "date-time"
        },
        "created_by": {
          "type": "string"
        },
        "updated_at": {
          "type": "string",
          "format": "date-time"
        },
        "updated_by": {
          "type": "string"
        }
      }
    },
    "match": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "tenants": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true,
          "description": "Tenant ids this rule applies to (empty = all)."
        },
        "topics": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true,
          "description": "Topics this rule applies to (empty = all)."
        },
        "risk_tags": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true,
          "description": "Required risk tags on the job context."
        }
      }
    },
    "decide": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "const": "throttle",
          "description": "Velocity rules always throttle."
        },
        "max_per_minute": {
          "type": "integer",
          "minimum": 1,
          "description": "Per-minute cap; rule fires when exceeded."
        },
        "max_per_hour": {
          "type": "integer",
          "minimum": 1,
          "description": "Per-hour cap."
        },
        "max_per_day": {
          "type": "integer",
          "minimum": 1,
          "description": "Per-day cap."
        },
        "burst_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "Allowed burst above the per-minute rate."
        },
        "reason": {
          "type": "string",
          "maxLength": 500,
          "description": "Human-readable rationale."
        }
      },
      "anyOf": [
        {
          "required": [
            "max_per_minute"
          ]
        },
        {
          "required": [
            "max_per_hour"
          ]
        },
        {
          "required": [
            "max_per_day"
          ]
        }
      ]
    },
    "description": {
      "type": "string"
    }
  }
}
